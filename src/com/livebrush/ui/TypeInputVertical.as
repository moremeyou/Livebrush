package com.livebrush.ui
{
	import flash.events.Event;
	import flash.display.MovieClip;
	import fl.data.DataProvider;
	import flash.display.Sprite;
	import fl.controls.TextInput;
	import flash.text.TextField;
	
	import com.livebrush.data.Settings;

	
	
	public class TypeInputVertical extends Sprite
	{
		
		public static var upperCaseLabels			:Boolean = true;
		
		
		private var _value							:String = "";
		
		
		public function TypeInputVertical ():void
		{
			super();
			
			init();
		}
		
		
		// GET/SET PROPS ////////////////////////////////////////////////////////////////////////////////////
		/////////////////////////////////////////////////////////////////////////////////////////////////////
		public function set list (l:Array):void {   _list.dataProvider = new DataProvider(l);   }
		public function set value (o:Object):void {   _list.selectedIndex = Settings.idToIndex(String(o), _list.dataProvider.toArray(), "data");   }
		public function get value ():Object {   return _list.selectedItem.data;   }
		public function get label ():String {   return _label.text;   }
		public function set label (s:String):void {   _label.text=(upperCaseLabels?s.toUpperCase():s);   }
		public function set enabled (b:Boolean) {   _list.enabled = b;   }
		public function get enabled ():Boolean {   return _list.enabled;   }
		
		
		// SETUP ////////////////////////////////////////////////////////////////////////////////////////////
		/////////////////////////////////////////////////////////////////////////////////////////////////////
		private function init ():void
		{
			//_list.addEventListener(Event.CHANGE, typeChange);
		}
		
		
		// EVENT LISTENERS //////////////////////////////////////////////////////////////////////////////////
		/////////////////////////////////////////////////////////////////////////////////////////////////////
		private function typeChange (e:Event):void
		{
			//dispatchEvent(new Event(Event.CHANGE));
		}
		
		
		// PUBLIC ACTIONS ///////////////////////////////////////////////////////////////////////////////////
		/////////////////////////////////////////////////////////////////////////////////////////////////////
		public function addType (o:Object):void
		{
			_list.addItem(o);
		}
		
		
		// UTILS ////////////////////////////////////////////////////////////////////////////////////////////
		/////////////////////////////////////////////////////////////////////////////////////////////////////
		
		
		// DEBUG ////////////////////////////////////////////////////////////////////////////////////////////
		/////////////////////////////////////////////////////////////////////////////////////////////////////


	}
	
	
}